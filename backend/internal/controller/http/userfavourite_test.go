package http

import "testing"

func TestAddUserToFavourites(T *testing.T) {
	app, dependencies := newTestApp(t)

	const route = "/api/v1/auth/login"
}

func TestGetFavouriteUsers(T *testing.T) {
	app, dependencies := newTestApp(t)

	const route = "/api/v1/auth/login"
}

func TestDeleteUserFromFavourites(T *testing.T) {
	app, dependencies := newTestApp(t)

	const route = "/api/v1/auth/login"
}
