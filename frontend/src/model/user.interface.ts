export interface User {
    id?: number,
    username?: string,
    firstName?: string,
    lastName?: string,
    description?: string,
    photo?: string,
    createdAt?: string,
    updatedAt?: string,
}