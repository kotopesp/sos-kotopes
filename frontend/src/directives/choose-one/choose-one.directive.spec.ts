import { ChooseOneDirective } from './choose-one.directive';

describe('ChooseOneDirective', () => {
  it('should create an instance', () => {
    const directive = new ChooseOneDirective();
    expect(directive).toBeTruthy();
  });
});
