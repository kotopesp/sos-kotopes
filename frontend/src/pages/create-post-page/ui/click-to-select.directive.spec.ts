import { ClickToSelectDirective } from './click-to-select.directive';

describe('ClickToSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickToSelectDirective();
    expect(directive).toBeTruthy();
  });
});
